# Neovim 使用说明

## 1. 基本约定

- Leader 键：`Space`
- 当前配置基于：
  - `lazy.nvim`
  - Neovim 原生 LSP
  - `nvim-cmp`
  - `nvim-treesitter`
  - `telescope.nvim`

---

## 2. 常用导航与编辑

### 行首 / 行尾 / 括号匹配

正常模式和可视模式都支持：

- `<leader>h`：跳到行首
- `<leader>l`：跳到行尾
- `<leader>b`：跳到匹配括号

### 快速上下移动

- `<C-j>`：向下跳 4 行
- `<C-k>`：向上跳 4 行
- `<C-d>`：向下跳 9 行
- `<C-u>`：向上跳 9 行

---

## 3. Buffer、文件树、终端

### Buffer 切换

- `<leader>bn`：下一个 buffer
- `<leader>bp`：上一个 buffer

### 文件树

- `<leader>nt`：打开/关闭文件树，并定位到当前文件

### 终端

- `<C-t>`：打开/关闭浮动终端

支持 normal / insert / terminal 模式切换。

---

## 4. 自动保存

### 开关

- `<leader>ua`：切换自动保存
- `:AutosaveToggle`：命令方式切换

### 触发时机

开启后会在以下时机自动保存：

- 离开插入模式
- 离开当前 buffer
- 窗口失焦

### 默认忽略

以下 filetype 不自动保存：

- `gitcommit`
- `gitrebase`

---

## 5. LSP 常用键位

### 跳转类

- `gd`：跳转定义
- `gD`：跳转声明
- `gi`：跳转实现
- `gY`：跳转类型定义
- `gr`：查看引用

### 文档与诊断

- `K`：查看 hover 文档
- `[g`：跳到上一个诊断
- `]g`：跳到下一个诊断
- `<leader>e`：查看当前行诊断浮窗
- `<leader>dq`：把诊断放到 loclist

### 修改代码

- `<leader>rn`：重命名符号
- `<leader>cf`：格式化
- `<leader>as`：Code Action
- `<leader>ac`：Code Action
- `<leader>qf`：Quick Fix
- `<leader>rf`：重构
- `<leader>rs`：重构

### 可视模式补充

- `<leader>as`：对选区执行 Code Action
- `<leader>rs`：对选区执行重构
- `<leader>cf`：格式化选区

### 符号与结构

- `gs`：当前文件符号列表
- `gc`：按当前光标单词搜索工作区符号

### C/C++ 专用

- `<leader>ch`：在源文件 / 头文件之间切换

### Inlay Hints

如果当前语言服务器支持：

- `<leader>uh`：切换 inlay hints

---

## 6. Diagnostics / Trouble

- `<leader>xx`：工作区诊断
- `<leader>xw`：当前 buffer 诊断
- `<leader>xq`：Quickfix 列表
- `<leader>xl`：Location List 列表

---
## 7. Telescope 搜索

- `<leader>ff`：搜索文件
- `<leader>fg`：全文搜索
- `<leader>fb`：列出当前 buffers
- `<leader>fh`：帮助文档 tags
- `<leader>fo`：最近打开文件
- `<leader>fp`：项目列表
- `<leader>fc`：搜索当前光标下单词

### Telescope 内部操作

在 Telescope 插入模式中：

- `<C-j>`：下一个候选项
- `<C-k>`：上一个候选项

---

## 8. 大纲 / 符号浏览

- `<leader>gv`：打开/关闭符号大纲

当前使用的是 `aerial.nvim`，后端支持：

- LSP
- Treesitter
- Markdown
- man

---

## 9. 注释

- `<leader>/`：注释当前行，或作为注释操作符使用

---

## 10. 补全系统

当前补全链路：

- `nvim-cmp`
- `cmp-nvim-lsp`
- `LuaSnip`
- `friendly-snippets`
- `nvim-autopairs`
- `lspkind`

### 插入模式键位

- `<C-Space>`：手动触发补全
- `<CR>`：确认补全
- `<C-e>`：取消补全
- `<C-b>`：文档上滚
- `<C-f>`：文档下滚
- `<Tab>`：
  - 补全菜单可见时选下一个
  - snippet 可跳转时前进
- `<S-Tab>`：
  - 补全菜单可见时选上一个
  - snippet 可跳转时后退

### 命令行补全

- `/`、`?`：buffer 内容补全
- `:`：命令行与路径补全

---

## 11. 当前启用的语言服务器

- `clangd`：C/C++
- `gopls`：Go
- `lua_ls`：Lua
- `pyright`：Python
- `bashls`：Shell/Bash/Zsh

---

## 12. Treesitter

当前已安装的 parser：

- `bash`
- `c`
- `cpp`
- `go`
- `json`
- `lua`
- `markdown`
- `markdown_inline`
- `python`
- `query`
- `vim`
- `vimdoc`

Treesitter 负责：

- 语法高亮启动
- 缩进表达式
- 折叠表达式
- 为部分结构插件提供支持

---

## 13. UI 与界面

### 主题

当前主题：`kanagawa`

### 状态栏

使用 `lualine`，显示内容包括：

- mode
- branch
- diff
- diagnostics
- 文件名
- symbol 路径
- encoding / fileformat / filetype
- progress / location

### Buffer 栏

使用 `bufferline`，支持：

- LSP diagnostics
- 对 `NvimTree` / `aerial` 的偏移显示

### 缩进线

使用 `indent-blankline.nvim` (`ibl`)。

---

## 14. 建议优先记住的键位

最常用的一组：

- `<leader>ff`：找文件
- `<leader>fg`：全文搜索
- `<leader>nt`：文件树
- `<C-t>`：终端
- `gd` / `gr` / `gi`：LSP 跳转
- `K`：查看文档
- `<leader>rn`：重命名
- `<leader>cf`：格式化
- `<leader>qf`：快速修复
- `[g` / `]g`：跳诊断
- `<leader>xx`：诊断面板
- `<leader>gv`：符号大纲
